/**
 * Slash command contract (to be registered against your Discord application):
 * /watch-session, /stop-watch, /status, /last-alerts
 *
 * This app exposes equivalent HTTP endpoints for these actions.
 */
export const supportedSlashCommands = ['/watch-session', '/stop-watch', '/status', '/last-alerts'];
